// Demo bypass route — same wizard as /diagnostic/.
// Convention: full diagnostic flows (questions + results, bypassing the landing
// page gate) live at /diagnostic-demo/ in all Aieutics tools.
export { default } from "../diagnostic/page";
